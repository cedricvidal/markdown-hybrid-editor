# esbuild's postinstall only resolves its platform binary; allowed so `pnpm install`
# is clean on every platform, not just the one where the optional dep happens to work.
# @playwright/browser-chromium downloads the browser @vscode/test-web runs against.
allowBuilds:
  esbuild: true
  "@playwright/browser-chromium": true
