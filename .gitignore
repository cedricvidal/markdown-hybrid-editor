node_modules/
dist/
# Root only: demo/out holds the committed walkthrough assets.
/out/
.vscode-test/
.vscode-test-web/
*.vsix

# Recording harness: frames are transient, rebuilt on every run.
demo/frames/
demo/out/take-*/
# Gate screenshots are regenerated on every run; the walkthrough assets
# (video, GIF and the numbered stills) are committed for the README.
demo/out/m[0-9]-*.png
demo/out/web*.png
# 2x capture masters for the README stills. The 1x deliverables they are
# resized into, in docs/media, are committed.
demo/out/hero/

.DS_Store
